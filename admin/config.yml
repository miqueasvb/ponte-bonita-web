# Configuración del Backend (Conexión con GitHub)
backend:
  name: github
  repo: miqueasvb/ponte-bonita-web # Tu repositorio
  branch: main # O master, según uses

# Dónde se guardan las imágenes que suba Barbi
media_folder: "assets/images/productos"
public_folder: "assets/images/productos"

# Configuración de las colecciones
collections:
  - name: "productos"
    label: "Productos (Joyas)"
    folder: "content/productos"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{title}}"
    fields:
      - { label: "Título del Producto", name: "title", widget: "string" }
      - { label: "Precio (sin el signo $)", name: "price", widget: "string" }
      - { label: "Imagen", name: "image", widget: "image" }
      - { 
          label: "Colección", 
          name: "section", 
          widget: "select", 
          options: ["Acero Blanco", "Acero Dorado"] 
        }
      - { 
          label: "Categoría", 
          name: "category", 
          widget: "select", 
          options: ["Anillos", "Pulseras", "Collares", "Aritos", "Cintos", "Kids", "Hombres"] 
        }
